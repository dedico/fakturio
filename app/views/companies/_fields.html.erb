<% semantic_form_for ([current_user, @company]) do |form| %>
    <%= form.inputs :name, :vat %>

    <% form.semantic_fields_for :address do |address| %>
        <%= address.inputs :street, :house_number, :postcode, :city, :countr %>
    <% end %>

    <%= form.buttons %>
<% end %>
